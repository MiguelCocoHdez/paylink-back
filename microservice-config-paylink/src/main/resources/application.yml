server:
  port: 8888

spring:
  profiles:
    active: native
  application:
    name: microservice-config-paylink
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config